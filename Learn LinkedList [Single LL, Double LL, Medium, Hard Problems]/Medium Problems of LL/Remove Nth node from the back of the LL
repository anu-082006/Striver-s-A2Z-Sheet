class Solution {
    public ListNode removeNthFromEnd(ListNode head, int n) {
        ListNode temp = head;
        int size = 0;
        while(temp != null) {
            size ++;
            temp = temp.next;
        }
        if(n==size) {
            return head.next;
        }how for i=0 size-n

        temp = head;
        ListNode prev = null;
        int index = size-n;
        for(int i=0; i<index; i++) {
            prev = temp;
            temp = temp.next;
        }
        prev.next = temp.next;
        return head;
    }
}
